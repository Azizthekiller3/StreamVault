// Legacy providers table — superseded by provider_sources + installed_extensions
// Kept as an empty router stub to avoid removing the DB table migration
import { Router } from "express";

const router = Router();

export default router;
